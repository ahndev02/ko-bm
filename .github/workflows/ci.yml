name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv python install
      - run: uv sync --extra dev --locked
      - run: uv run pytest -m "not gpu" -q --cov=kobalt_eval --cov-report=term
